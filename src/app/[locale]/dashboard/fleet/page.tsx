import { ComingSoon } from "@/components/coming-soon";

export default function FleetPage() {
  return (
    <ComingSoon
      title="Fleet overview"
      note="A combined vehicles + drivers utilization view (assignment status, maintenance schedule). Vehicles and Drivers are already manageable from their own pages; let me know what fleet metrics matter most and I'll build this dashboard."
    />
  );
}
