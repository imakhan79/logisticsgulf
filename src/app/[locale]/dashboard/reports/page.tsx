import { ComingSoon } from "@/components/coming-soon";

export default function ReportsPage() {
  return (
    <ComingSoon
      title="Reports"
      note="Deeper analytics beyond the Overview chart (revenue, on-time delivery rate, expense breakdown by vehicle). Tell me which reports matter most and I'll build them with Recharts against the existing tables."
    />
  );
}
